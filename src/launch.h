#ifndef _LAUNCH_H
#define _LAUNCH_H

// launch program inside a directory (prg name == directory name)
void launch(char* dir, char* name);

// launch a prg directly
void launchPrg(char* name);

#endif